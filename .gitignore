# Godot generated files (re-created on import; not source)
.godot/

# macOS
.DS_Store

# Local MCP server config (contains an API token — never commit)
.mcp.json

# LDtk keeps timestamped copies of the project here on every save.
ldtk/*/backups/

# Exported builds (regenerated by --export-release; not source)
builds/
